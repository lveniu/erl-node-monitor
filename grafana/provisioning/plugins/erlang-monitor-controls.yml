apiVersion: 1

apps:
  - type: erlang-monitor-controls-app
    org_id: 1
    disabled: false
    jsonData:
      collectorUrl: http://erlang-exporter:20903
      opsAgentUrl: http://erlang-ops-agent:20906
      # Replace with an address reachable from the Grafana server/container.
      codeMcpUrl: http://127.0.0.1:3100
    secureJsonData:
      opsAgentToolToken: $OPS_AGENT_TOOL_API_TOKEN
      codeMcpToken: $CODE_MCP_AUTH_TOKEN
