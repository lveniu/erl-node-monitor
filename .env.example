# 在192.168.100.22正式部署并完成防火墙限制后，可改为该内网IP。
MONITOR_BIND_IP=127.0.0.1
# Used by the optional compose.ops-agent.yml overlay; keep real values in
# secrets/ops_agent_model_api_key and secrets/ops_agent_tool_api_token.
